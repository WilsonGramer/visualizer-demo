# yaml-language-server: $schema=_schema.json
as: trait
rule: trait definition
where:
  - child:
      as: node
      rules:
        - unresolved trait
  - type:
      of: node
      as: ty
group: todo
messages:
  - node: node
    template: |
      Could not find an instance for `{{trait:source}}` with type {{ty:type}}.
