# yaml-language-server: $schema=_schema.json
as: placeholder
rule: placeholder
where:
  - type:
      as: ty
group: todo
messages:
  - node: placeholder
    template: |
      Found a placeholder for {{ty:type}}.
  - node: placeholder
    template: |
      Replace this placeholder with a {{ty:type}} value before running your code.
  - type: ty
    template: |
      The placeholder is a {{ty:type}}...
