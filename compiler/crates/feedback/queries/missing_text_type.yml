# yaml-language-server: $schema=_schema.json
as: literal
rule: missing text type
group: todo
messages:
  - node: literal
    template: |
      Can't create `{{literal:source}}` because the `Text` type is missing.
  - node: literal
    template: |
      Make sure to add `Text : type` at the top of your code.
